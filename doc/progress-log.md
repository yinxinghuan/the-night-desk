# Portrait Quest 进展日志

## 2026-07-14

### 已完成

- 确认项目目标、资产成本边界与好友列表暂缓范围。
- 查证用户名反查接口仍可返回 `ghostpixel` 的平台身份与头像。
- 对动作、滤镜、视觉小说、卡牌、推理与决策生存六种形态进行了首轮评估。
- 盘点 `last-pour`、`replicant-wake`、`the-confession` 与 `swipe-storm` 的可复用经验。
- 选择“肖像驱动决策生存”作为第一母模板方向。
- 建立独立项目目录 `portrait-quest`，确定首个验证主题 The Night Desk。
- 完成 12 回合可玩循环、18 张事件牌、条件分支、四项状态与 9 类结局的首版实现。
- 首次 `npm run build` 已通过。
- 完成 390 × 844 手机画幅的开始页、游戏页与选择反馈截图检查。
- 通过 Aigram transit 生图接口完成 ghostpixel 第二版角色锚点；第一版因衣服误生成文字被主动废弃。
- 将角色包生成脚本改为记录每次 transit 请求并支持中断续跑，避免重复生成已完成资产。
- 完成 7 张多状态去背立绘并通过统一接触表检查。
- 完成 intro / midpoint / win / lose 四张竖屏关键剧情图并接入 Identity Pack。
- 完成中文与英文手机画幅检查，页面在 390 × 844 下无横向或纵向溢出。
- 自动跑通三条完整路径：交替选择完成 12 个不重复事件并胜利；全左与全右均在第 11 回合因不同状态归零失败；三局控制台错误为 0。
- 生成 1024 × 1024 正式叙事封面并检查 160 × 160 缩略图，标题、主角和核心情境仍清晰。
- 将发布资产与制作期文件分离：最终图保留在 `public/`，绿幕、比例参考和 transit 日志迁移到 `_artifacts/`。
- 补充 Identity Pack 生产规范与最终技术文档。
- 文档复读发现并补齐危机低频脉冲、12 个反馈粒子、280 ms 镜头抖动、900 ms 结局光晕与 420 ms 结果面板滑入；同时把结果短句显示提前到 220 ms，保留至少 880 ms 阅读时间。
- 补效后重新跑到 12 个不重复事件的胜利结局 `THE PERFECT NIGHT MANAGER`，控制台错误为 0；另保留一条第 10 回合镇定归零的随机失败样本，证明混合策略也不是机械必赢。
- 收到第二位真实用户 Isabel 的 Aigram 主页截图；用户名接口没有返回资料，明确改用截图头像且不设置虚构平台 ID。
- 完成 Isabel ×《秋日失物亭》替换需求：18 张新事件、四项新状态语义、双语 copy、双中点与 9 类结局。
- 完成 Isabel 角色锚点、7 张去背状态立绘和 intro / midpoint / win / lose 四张全屏图；全部 transit 请求有制作日志。
- 增加透明裁边与 28 px 安全边距处理，修复人类立绘在共享容器内显得过小的问题；游戏中使用 270 × 306 近景构图。
- 将 UI 中 `04:44`、`ON DUTY`、状态标签和主题最高分硬编码移入 Theme Cartridge；关键发现和特殊结局标记也改为卡带配置。
- 使用 `?variant=isabel` 提供并列实验入口，默认 ghostpixel 版本保持不变。
- 两个版本均在 390 × 844 无头 Chromium 中完成 12 次选择并进入结局；控制台错误 0、页面无滚动溢出、人物和场景路径无交叉。

### 当前进行中

- 无；第二轮人物 × 主题替换实验已完成并通过本地验证。

### 下一步

- 用第三个宠物或抽象头像测试非人类 Identity Lock；人类样本已完成。
- 正式把当前 `IdentityPack` 拆为 SourceIdentity / RolePortraitPack / NarrativeScenePack，避免误导跨主题资产复用成本。
- 在获得发布指令后生成独立 UUID、仓库、zipurl 与平台条目。
